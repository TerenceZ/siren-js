{
  "middleware": {

  	"responseTime": {
  		"enabled": true
  	},

    "logger": {
      "enabled": true
    },

    "shutdown": {
      // Clean shutdowns aren't necessary in `dev` mode
      "enabled": false
    }
  }
}